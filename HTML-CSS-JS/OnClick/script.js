function changeName(element) {
    console.log(element.innerText="Login")
    if(element.innerText="Login") {
        element.innerText="Logout"
    }
    else {
        element.innerText="Login"
    }
}

function eliminar(evento) {
    evento.remove()
}